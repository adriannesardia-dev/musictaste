{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        "COMPOSER_MEMORY_LIMIT=-1 composer install --no-dev --optimize-autoloader --no-scripts --no-interaction"
      ]
    }
  }
}